# ANTLRBasics
Repo for my compilers class to learn the basic concepts of antlr

# If you want to try it run 
`python3 client.py`
# Be careful with spaces and syntax
## if:
if (exp) then {exp} else {exp}
